const { findEmail, insertUser } = require('./usersModel');
const { allRecipes, insertRecipe } = require('./recipesModel');

module.exports = {
  findEmail,
  insertUser,
  allRecipes,
  insertRecipe,
};
