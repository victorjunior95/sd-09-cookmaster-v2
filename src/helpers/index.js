const checkUserData = require('./checkUserData');
const { checkLoginData, checkEmailAndPassword } = require('./checkLoginData');

module.exports = {
  checkUserData,
  checkLoginData,
  checkEmailAndPassword,
};